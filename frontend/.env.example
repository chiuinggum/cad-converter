# Copy wondercad/.env to this folder for local dev, or set vars in wondercad/.env (recommended).
# GEMINI_API_KEY and GEMINI_MODEL are shared by the Node server, Pro-CAD agent, and drawing agent.

GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-3.5-flash
